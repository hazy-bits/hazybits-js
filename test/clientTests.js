const HazyBitsClient = require(__dirname + '/../index.js');

describe('HazyBits Client Tests', function(){
  this.timeout(20000);
  this.slow(5000);

  it('should connect successfully', function(done) {
    done();
  });
});
